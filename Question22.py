for i in range(1,31)
 list.append(i)
multiples_of_5=[num for num in list if num%5==0]
multiples_of_3=[num for num in list if num%3==0]
multiples_of_5and3=[num for num in list if num%5==0 && num%3==0]
